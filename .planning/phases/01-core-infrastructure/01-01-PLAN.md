---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FlightAgent.Api/Program.cs
  - src/FlightAgent.Infrastructure/DependencyInjection.cs
  - src/FlightAgent.Api/FlightAgent.Api.csproj
autonomous: true
requirements:
  - REQ-009
must_haves:
  truths:
    - "GET /health returns service health status"
    - "Health check includes external API connectivity verification"
    - "Returns 200 when healthy, 503 when degraded"
    - "Clean Architecture DI is properly wired"
  artifacts:
    - path: "src/FlightAgent.Api/Program.cs"
      provides: "DI registration and health endpoint"
      min_lines: 40
    - path: "src/FlightAgent.Infrastructure/DependencyInjection.cs"
      provides: "Service registrations"
      exports: ["AddInfrastructure"]
    - path: "src/FlightAgent.Api/FlightAgent.Api.csproj"
      provides: "Health checks package reference"
      contains: "AspNetCore.HealthChecks"
  key_links:
    - from: "src/FlightAgent.Api/Program.cs"
      to: "src/FlightAgent.Infrastructure/DependencyInjection.cs"
      via: "builder.Services.AddInfrastructure()"
      pattern: "AddInfrastructure"
    - from: "src/FlightAgent.Api/Program.cs"
      to: "/health endpoint"
      via: "MapHealthChecks"
      pattern: "MapHealthChecks|/health"
---

<objective>
Wire up Clean Architecture dependency injection and implement health check endpoint with external API connectivity verification.

Purpose: Establish the foundational infrastructure for the FlightAgent API, ensuring proper service registration and deployment verification capability.
Output: Working health endpoint at `/health` that validates service and external API status.
</objective>

<execution_context>
@D:/Project/FlightAgent/.claude/get-shit-done/workflows/execute-plan.md
@D:/Project/FlightAgent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Project/FlightAgent/.planning/REQUIREMENTS.md
@D:/Project/FlightAgent/.planning/codebase/ARCHITECTURE.md
@D:/Project/FlightAgent/.planning/codebase/STRUCTURE.md
@D:/Project/FlightAgent/.planning/codebase/CONVENTIONS.md

## Existing Code Context

From src/FlightAgent.Api/Program.cs (current):
- Has OpenAPI configuration
- Has sample `/weatherforecast` endpoint
- Missing `AddInfrastructure()` call
- No health checks configured

From src/FlightAgent.Infrastructure/DependencyInjection.cs:
- `AddInfrastructure()` extension method exists
- Registers Semantic Kernel and services
- Uses scoped lifetime for services, singleton for Kernel
- Pattern: `services.AddScoped<IFlightSearchService, SemanticFlightSearchService>();`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add health check package and update DI registration</name>
  <files>src/FlightAgent.Api/FlightAgent.Api.csproj, src/FlightAgent.Api/Program.cs, src/FlightAgent.Infrastructure/DependencyInjection.cs</files>
  <action>
    1. Add Microsoft.Extensions.Diagnostics.HealthChecks package to FlightAgent.Api.csproj:
       - Add PackageReference with Version 9.0.0 (compatible with .NET 10)

    2. Update src/FlightAgent.Api/Program.cs:
       - Add `builder.Services.AddInfrastructure(builder.Configuration);` before `var app = builder.Build();`
       - Add health checks registration: `builder.Services.AddHealthChecks();`
       - Map health endpoint: `app.MapHealthChecks("/health");` before `app.Run();`
       - Keep existing OpenAPI and weatherforecast endpoint

    3. Verify src/FlightAgent.Infrastructure/DependencyInjection.cs has proper registrations:
       - Should register IFlightSearchService and IBookingService as scoped
       - Should register Kernel as singleton
       - No changes needed if already correct per existing file

    Follow existing patterns:
    - Use 4-space indentation (K&R style)
    - Implicit usings enabled, no explicit usings for System namespaces
    - Use PascalCase for method names
  </action>
  <verify>
    <automated>cd D:/Project/FlightAgent && dotnet build --no-restore</automated>
  </verify>
  <done>FlightAgent.Api.csproj has health checks package, Program.cs calls AddInfrastructure(), health checks registered, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Add custom health check for external API connectivity</name>
  <files>src/FlightAgent.Infrastructure/Health/ExternalApiHealthCheck.cs, src/FlightAgent.Infrastructure/DependencyInjection.cs</files>
  <action>
    1. Create new directory: src/FlightAgent.Infrastructure/Health/

    2. Create src/FlightAgent.Infrastructure/Health/ExternalApiHealthCheck.cs:
       - Implement IHealthCheck interface
       - Create class ExternalApiHealthCheck with constructor-injected IHttpClientFactory and ILogger<ExternalApiHealthCheck>
       - In CheckHealthAsync method:
         - Try to fetch from a reliable external endpoint (e.g., https://api.github.com or httpbin.org/get)
         - Return HealthCheckResult.Healthy() if HTTP 200 received within 5 seconds
         - Return HealthCheckResult.Degraded() or Unhealthy() on timeout/error
         - Include description: "External API connectivity check"
         - Include exception message in data dictionary if failed

    3. Update src/FlightAgent.Infrastructure/DependencyInjection.cs:
       - Add `services.AddHttpClient();` for IHttpClientFactory
       - Change health checks registration to custom:
         ```csharp
         services.AddHealthChecks()
             .AddCheck<Health.ExternalApiHealthCheck>("external_api", tags: new[] { "external" });
         ```

    Follow conventions:
    - Use file-scoped namespace declaration
    - Use PascalCase for class names
    - Use expression-bodied members where appropriate
    - Add XML doc comments for public class
  </action>
  <verify>
    <automated>cd D:/Project/FlightAgent && dotnet build --no-restore</automated>
  </verify>
  <done>ExternalApiHealthCheck class exists, implements IHealthCheck, registered in DI, build succeeds</done>
</task>

<task type="auto">
  <name>Task 3: Configure health check response and verify endpoint</name>
  <files>src/FlightAgent.Api/Program.cs</files>
  <action>
    1. Update src/FlightAgent.Api/Program.cs health endpoint mapping:
       - Replace simple `app.MapHealthChecks("/health")` with configured version:
         ```csharp
         app.MapHealthChecks("/health", new HealthCheckOptions
         {
             ResponseWriter = async (context, report) =>
             {
                 context.Response.ContentType = "application/json";
                 var result = new
                 {
                     status = report.Status.ToString(),
                     checks = report.Entries.Select(e => new
                     {
                         name = e.Key,
                         status = e.Value.Status.ToString(),
                         description = e.Value.Description,
                         duration = e.Value.Duration.ToString()
                     }),
                     totalDuration = report.TotalDuration.ToString()
                 };
                 await context.Response.WriteAsJsonAsync(result);
             }
         });
         ```

    2. Ensure proper using statements at top of file (if not using implicit usings):
       - using Microsoft.AspNetCore.Diagnostics.HealthChecks;
       - using System.Text.Json;

    3. Build and test the endpoint:
       - Run `dotnet run` in background or use `curl` after build
       - Verify endpoint returns JSON response
  </action>
  <verify>
    <automated>cd D:/Project/FlightAgent && dotnet build</automated>
  </verify>
  <done>Health endpoint at /health returns JSON with status, checks array, and duration; returns appropriate HTTP status code (200 healthy, 503 degraded). Test manually: run `dotnet run` then visit http://localhost:5120/health</done>
</task>

</tasks>

<verification>
1. Build succeeds: `dotnet build` completes with no errors
2. Health endpoint accessible: `GET /health` returns JSON response
3. Response includes:
   - Overall status field (Healthy/Degraded/Unhealthy)
   - Checks array with individual check results
   - External API check with description
   - Total duration
4. HTTP status codes:
   - 200 OK when all checks pass
   - 503 Service Unavailable when external API check fails
5. DI wiring verified:
   - AddInfrastructure() called and services resolve correctly
   - IFlightSearchService and IBookingService registered as scoped
</verification>

<success_criteria>
- [ ] `GET /health` endpoint returns JSON response with status, checks, and duration
- [ ] Health check includes external API connectivity verification
- [ ] Returns HTTP 200 when healthy, HTTP 503 when external API unavailable
- [ ] `builder.Services.AddInfrastructure(builder.Configuration)` is called in Program.cs
- [ ] All projects build successfully (`dotnet build` succeeds)
- [ ] Clean Architecture DI flow established: Api -> Infrastructure -> Core
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md` with:
- Phase: 01-core-infrastructure
- Plan: 01
- Goal: Wire up Clean Architecture DI and implement health check endpoint
- Affects: src/FlightAgent.Api/Program.cs, src/FlightAgent.Infrastructure/DependencyInjection.cs, src/FlightAgent.Infrastructure/Health/ExternalApiHealthCheck.cs
- Provides: Health endpoint at /health, proper DI registration, external API connectivity monitoring
- Tech Stack: ASP.NET Core Health Checks, IHealthCheck interface, HttpClient
- Patterns: Extension method registration (AddInfrastructure), custom health check implementation, JSON response writer
- Decisions: Use httpbin.org/get for external API check (reliable, no auth required), JSON response format for health status
</output>
