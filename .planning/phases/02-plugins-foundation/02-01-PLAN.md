---
phase: 02-plugins-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FlightAgent.Core/Interfaces/IAirportPlugin.cs
  - src/FlightAgent.Core/Interfaces/IWeatherPlugin.cs
  - src/FlightAgent.Core/Models/AirportInfo.cs
  - tests/FlightAgent.Infrastructure.Tests/FlightAgent.Infrastructure.Tests.csproj
  - tests/FlightAgent.Infrastructure.Tests/Plugins/AirportPluginTests.cs
  - tests/FlightAgent.Infrastructure.Tests/Plugins/WeatherPluginTests.cs
autonomous: true
requirements:
  - REQ-006
  - REQ-007
must_haves:
  truths:
    - Plugin interfaces exist and define expected method signatures
    - AirportInfo record type exists with all required fields
    - Test project exists with Moq and FluentAssertions configured
  artifacts:
    - path: src/FlightAgent.Core/Interfaces/IAirportPlugin.cs
      provides: Interface for DI and Moq
      exports: ["IAirportPlugin", "GetAirportInfoAsync"]
    - path: src/FlightAgent.Core/Interfaces/IWeatherPlugin.cs
      provides: Interface for DI and Moq
      exports: ["IWeatherPlugin", "GetAirportWeatherAsync"]
    - path: src/FlightAgent.Core/Models/AirportInfo.cs
      provides: Airport data record
      exports: ["AirportInfo"]
    - path: tests/FlightAgent.Infrastructure.Tests/FlightAgent.Infrastructure.Tests.csproj
      provides: Test project configuration
    - path: tests/FlightAgent.Infrastructure.Tests/Plugins/AirportPluginTests.cs
      provides: Test scaffold for AirportPlugin
    - path: tests/FlightAgent.Infrastructure.Tests/Plugins/WeatherPluginTests.cs
      provides: Test scaffold for WeatherPlugin
  key_links:
    - from: IAirportPlugin
      to: AirportPlugin
      via: DI registration (AddSingleton)
    - from: IWeatherPlugin
      to: WeatherPlugin
      via: DI registration (AddSingleton)
---

<objective>
Define plugin contracts and create test infrastructure for Phase 2.

Purpose: Establish the interfaces and models that AirportPlugin and WeatherPlugin will implement, enabling testability via Moq and providing clear contracts for DI registration.

Output: Interfaces (IAirportPlugin, IWeatherPlugin), AirportInfo record, and test project with scaffolded tests.
</objective>

<execution_context>
@D:/Project/FlightAgent/.claude/get-shit-done/workflows/execute-plan.md
@D:/Project/FlightAgent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-plugins-foundation/02-CONTEXT.md
@.planning/phases/02-plugins-foundation/02-RESEARCH.md
@.planning/REQUIREMENTS.md

## Decision Coverage
This plan implements:
- D-02: Use separate specific interfaces (IAirportPlugin, IWeatherPlugin) for testability via Moq
- D-08: AirportInfo record type with IataCode, Name, City, Country, Lat, Lon, Timezone
- D-29: Thin interfaces matching public method signatures
- D-30: Interfaces are for DI and Moq only

## Existing Patterns
From src/FlightAgent.Core/Interfaces/IFlightSearchService.cs:
- Interfaces in FlightAgent.Core/Interfaces/
- Async methods with CancellationToken defaults

From src/FlightAgent.Core/Models/Flight.cs:
- Models in FlightAgent.Core/Models/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin interfaces</name>
  <files>
    src/FlightAgent.Core/Interfaces/IAirportPlugin.cs
    src/FlightAgent.Core/Interfaces/IWeatherPlugin.cs
  </files>
  <action>
Create two interface files per D-29:

1. IAirportPlugin.cs:
   - Method: GetAirportInfoAsync(string iataCode, CancellationToken ct = default)
   - Return: Task<string?>
   - XML doc comment describing IATA code lookup

2. IWeatherPlugin.cs:
   - Method: GetAirportWeatherAsync(string iataCode, CancellationToken ct = default)
   - Return: Task<string?>
   - XML doc comment describing weather lookup by airport code

Both interfaces:
- Must be public
- Must have CancellationToken with default(CancellationToken)
- Must return nullable string (string?) per D-19

DO NOT:
- Add [KernelFunction] attributes to interfaces (D-04: attributes stay on concrete classes only)
- Create a common IPlugin base interface (D-01: no common interface)
  </action>
  <verify>
    <automated>grep -l "interface IAirportPlugin" src/FlightAgent.Core/Interfaces/*.cs && grep -l "interface IWeatherPlugin" src/FlightAgent.Core/Interfaces/*.cs</automated>
  </verify>
  <done>
    - Both interface files exist with correct method signatures
    - Methods return Task<string?>
    - CancellationToken has default value
    - No KernelFunction attributes on interfaces
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AirportInfo record</name>
  <files>src/FlightAgent.Core/Models/AirportInfo.cs</files>
  <action>
Create AirportInfo.cs per D-08:

```csharp
public record AirportInfo(
    string IataCode,
    string Name,
    string City,
    string Country,
    double Lat,
    double Lon,
    string Timezone);
```

Per D-06, D-09:
- IataCode: 3-letter code (JFK, LHR)
- Lat/Lon: Coordinates as double (mandatory for WeatherPlugin per D-09)
- Timezone: IANA timezone identifier (America/New_York)

Place in FlightAgent.Core.Models namespace.
  </action>
  <verify>
    <automated>grep -l "record AirportInfo" src/FlightAgent.Core/Models/*.cs</automated>
  </verify>
  <done>
    - AirportInfo record exists with all 7 properties
    - Properties are in correct order per D-08
    - File compiles (dotnet build src/FlightAgent.Core/FlightAgent.Core.csproj)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test project and scaffold</name>
  <files>
    tests/FlightAgent.Infrastructure.Tests/FlightAgent.Infrastructure.Tests.csproj
    tests/FlightAgent.Infrastructure.Tests/Plugins/AirportPluginTests.cs
    tests/FlightAgent.Infrastructure.Tests/Plugins/WeatherPluginTests.cs
  </files>
  <action>
Create test project following .NET testing patterns:

1. Create csproj with packages:
   - Microsoft.NET.Test.Sdk (17.12.0)
   - xunit (2.9.2)
   - xunit.runner.visualstudio (2.8.2)
   - Moq (4.20.72) - for mocking interfaces
   - FluentAssertions (7.0.0) - per REQ-008
   - Microsoft.Extensions.Caching.Memory (9.0.0)
   - Reference FlightAgent.Infrastructure and FlightAgent.Core

2. Create Plugins directory with test scaffold files:
   - AirportPluginTests.cs: Empty test class with Arrange/Act/Assert comments
   - WeatherPluginTests.cs: Empty test class with Arrange/Act/Assert comments

3. Add placeholder test methods:
   - AirportPluginTests: GetAirportInfoAsync_ReturnsInfo_ForValidIataCode (commented or throws NotImplementedException)
   - WeatherPluginTests: GetAirportWeatherAsync_ReturnsWeather_ForValidIataCode (commented or throws NotImplementedException)

This is Wave 0 scaffolding - tests will be implemented in Phase 6 per REQ-008.
  </action>
  <verify>
    <automated>dotnet restore tests/FlightAgent.Infrastructure.Tests/FlightAgent.Infrastructure.Tests.csproj</automated>
  </verify>
  <done>
    - Test project csproj exists with all required packages
    - Test files scaffolded with placeholder methods
    - Project restores successfully
    - References FlightAgent.Infrastructure and FlightAgent.Core
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | No external calls in this plan - interfaces only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | IataCode parameter | accept | IATA codes are public information, not sensitive |
| T-02-02 | Repudiation | Interface method signatures | accept | Logging will be handled in implementation (Phase 2.2) |

No active mitigations required - this plan only defines contracts.
</threat_model>

<verification>
## Build Verification
```bash
dotnet build src/FlightAgent.Core/FlightAgent.Core.csproj
dotnet build tests/FlightAgent.Infrastructure.Tests/FlightAgent.Infrastructure.Tests.csproj
```

## Interface Verification
- IAirportPlugin has GetAirportInfoAsync method
- IWeatherPlugin has GetAirportWeatherAsync method
- Both return Task<string?>
- AirportInfo has 7 properties (IataCode, Name, City, Country, Lat, Lon, Timezone)
</verification>

<success_criteria>
- Plugin interfaces exist with correct signatures per D-29
- AirportInfo record defined per D-08
- Test project created with Moq and FluentAssertions per REQ-008
- All files compile successfully
- Interfaces placed in FlightAgent.Core/Interfaces/
- Models placed in FlightAgent.Core/Models/
</success_criteria>

<output>
After completion, create `.planning/phases/02-plugins-foundation/02-01-SUMMARY.md`
</output>
