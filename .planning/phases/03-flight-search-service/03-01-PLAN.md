---
phase: 03-flight-search-service
plan_number: 01
name: flight-info-models
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-002
files_modified:
  - src/FlightAgent.Core/Models/FlightInfo.cs
  - src/FlightAgent.Infrastructure/Models/AviationStackResponse.cs
verification_steps:
  - dotnet build src/FlightAgent.Core/FlightAgent.Core.csproj
  - dotnet build src/FlightAgent.Infrastructure/FlightAgent.Infrastructure.csproj
---

<objective>
Create FlightInfo record and AviationStack API response DTOs for Phase 3.
</objective>

<tasks>

<task>
  <name>Create FlightInfo record</name>
  <file>src/FlightAgent.Core/Models/FlightInfo.cs</file>
  <action>
Create FlightInfo.cs in FlightAgent.Core/Models/:

```csharp
namespace FlightAgent.Core.Models;

public record FlightInfo(
    string FlightNumber,
    string Airline,
    string DepartureAirport,
    string ArrivalAirport,
    DateTime ScheduledDeparture,
    DateTime? ActualDeparture,
    string Status,
    int DelayMinutes);
```

Per D-04, include all fields: flight_number (FlightNumber), airline, departure_airport, arrival_airport, scheduled_departure, actual_departure, status, delay_minutes.

Status values: "on_time", "delayed", "cancelled", "unknown"
</action>
  <verify>
grep -l "record FlightInfo" src/FlightAgent.Core/Models/*.cs
  </verify>
</task>

<task>
  <name>Create AviationStack response DTOs</name>
  <file>src/FlightAgent.Infrastructure/Models/AviationStackResponse.cs</file>
  <action>
Create DTOs matching AviationStack API response structure (per RESEARCH Pattern 2):

```csharp
using System.Text.Json.Serialization;

namespace FlightAgent.Infrastructure.Models;

public class AviationStackResponse
{
    [JsonPropertyName("data")]
    public List<FlightData> Data { get; set; } = new();
}

public class FlightData
{
    [JsonPropertyName("flight_date")]
    public string FlightDate { get; set; } = "";

    [JsonPropertyName("flight_status")]
    public string? FlightStatus { get; set; }

    [JsonPropertyName("departure")]
    public AirportTimes Departure { get; set; } = new();

    [JsonPropertyName("arrival")]
    public AirportTimes Arrival { get; set; } = new();

    [JsonPropertyName("airline")]
    public AirlineInfo Airline { get; set; } = new();

    [JsonPropertyName("flight")]
    public FlightIdentifier Flight { get; set; } = new();
}

public class AirportTimes
{
    [JsonPropertyName("iata")]
    public string Iata { get; set; } = "";

    [JsonPropertyName("airport")]
    public string Airport { get; set; } = "";

    [JsonPropertyName("delay")]
    public int Delay { get; set; }

    [JsonPropertyName("scheduled")]
    public string Scheduled { get; set; } = "";

    [JsonPropertyName("actual")]
    public string? Actual { get; set; }
}

public class AirlineInfo
{
    [JsonPropertyName("name")]
    public string Name { get; set; } = "";

    [JsonPropertyName("iata")]
    public string Iata { get; set; } = "";
}

public class FlightIdentifier
{
    [JsonPropertyName("number")]
    public string Number { get; set; } = "";

    [JsonPropertyName("iata")]
    public string Iata { get; set; } = "";
}
```

All properties use JsonPropertyName for System.Text.Json mapping.
</action>
  <verify>
grep -l "class AviationStackResponse" src/FlightAgent.Infrastructure/Models/*.cs
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External API response | AviationStack JSON — validate structure before use |

## STRIDE Threat Register

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-03-01 | Information Disclosure | accept | Flight data is public info; no PII in FlightInfo |
</threat_model>

<success_criteria>
- FlightInfo record has all 8 fields per D-04
- AviationStackResponse DTOs map API response structure
- All files compile successfully
</success_criteria>