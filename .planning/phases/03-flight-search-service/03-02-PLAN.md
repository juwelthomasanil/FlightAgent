---
phase: 03-flight-search-service
plan_number: 02
name: aviation-stack-service-interface
type: execute
wave: 1
depends_on:
  - 03-01
autonomous: true
requirements:
  - REQ-002
files_modified:
  - src/FlightAgent.Core/Interfaces/IAviationStackService.cs
verification_steps:
  - dotnet build src/FlightAgent.Core/FlightAgent.Core.csproj
---

<objective>
Define IAviationStackService interface for API calls and Moq testing.
</objective>

<tasks>

<task>
  <name>Create IAviationStackService interface</name>
  <file>src/FlightAgent.Core/Interfaces/IAviationStackService.cs</file>
  <action>
Create interface following established pattern (IAirportPlugin, IWeatherPlugin):

```csharp
namespace FlightAgent.Core.Interfaces;

public interface IAviationStackService
{
    Task<FlightInfo?> GetFlightStatusAsync(
        string flightNumber,
        string date,
        CancellationToken cancellationToken = default);
}
```

Per D-29 pattern: thin interface matching public method signatures, async with CancellationToken default.
</action>
  <verify>
grep -l "interface IAviationStackService" src/FlightAgent.Core/Interfaces/*.cs
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Interface only — no external calls |

## STRIDE Threat Register

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-03-02 | Information Disclosure | accept | No sensitive data in interface definition |
</threat_model>

<success_criteria>
- IAviationStackService has GetFlightStatusAsync method
- Method signature: (string flightNumber, string date, CancellationToken ct = default)
- Returns Task<FlightInfo?>
- File compiles successfully
</success_criteria>